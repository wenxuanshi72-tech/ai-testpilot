# Local Runtime Artifacts

The subdirectories under this root are retained so later local runs have stable boundaries. Their real contents are ignored by Git; only explanatory `.gitkeep` markers are versioned in Phase 1.

No report, bug, evidence, export, or log in this directory represents a completed run unless a later phase creates and validates it with the required manifest and trace links.
