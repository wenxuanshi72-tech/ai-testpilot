# Versioned Prompts

Future prompt sources are grouped by use case and must carry explicit versions, compatible schema versions, and real/mock provenance. Phase 1 creates no operational prompt and performs no model call.
