Repair only the structural outline contract. Return exactly one complete JSON object. Preserve the
document summary, section titles, source headings, section order, and section count. Every
section_id must be unique and match ^SEC-[A-Za-z0-9_-]{1,64}$; use stable IDs such as SEC-001.
Do not add or remove sections, invent source headings, or return prose.
