This is one bounded recovery attempt after a source-grounding failure. Regenerate only the supplied
batch. Copy source_block_id exactly. Copy source_excerpt as one uninterrupted verbatim substring
from that block. Do not paraphrase, translate, normalize, combine lines from separate spans, or
cite text outside the blocks. When continuous support is absent, use unsupported with reason
no_continuous_source; never fabricate support. Return only the required JSON object.
