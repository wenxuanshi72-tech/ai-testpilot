This is a bounded failed-run recovery using a new versioned field contract. Generate only the supplied batch and preserve batch_id and case_type. Return one complete JSON object without Markdown. Do not reuse the failed response as candidate data, and do not refer to, regenerate, or alter any compatible validated checkpoint. Correct only by following the current explicit field contract; never invent requirements, expected outcomes, secrets, executable code, or PASS/FAIL. No automatic retry occurs when max_retries is zero.
