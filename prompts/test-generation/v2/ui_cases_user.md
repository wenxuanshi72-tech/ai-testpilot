Create UI Test Intents for batch {{batch_id}}.
Formal UI contract: {{ui_contract}}
Requirements: {{requirements_json}}
Return exactly the JSON root contract from the system message. Use unsupported rather than guessing.
