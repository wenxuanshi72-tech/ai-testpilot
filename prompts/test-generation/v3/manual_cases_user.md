Generate semantic test intents for these immutable system-owned slots:
{{slots_json}}
Approved API contract: {{api_contract}}
Approved UI contract: {{ui_contract}}
Return one intent per slot, no omissions and no duplicates. The only identifier you may return is generation_slot_id.
