Fix {{validation_error}} under {{intent_schema_version}}; preserve semantics. {{type_recovery_rules}} API config setup is a string, never a type=config object. UI actions use goto:route, fill:label, or click:role with real accessible names; never navigate, Submit, or Content. Return one complete JSON object for supplied slot IDs only; no system fields, Markdown, or explanation.
