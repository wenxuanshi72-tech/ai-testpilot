# Versioned Schemas

Future JSON Schemas for requirements, test cases, execution results, and bugs live in the corresponding subdirectories. Phase 1 keeps only the governed directory boundary; no production schema is defined early.
